// emotion
import { css } from "@emotion/css";

const span = css({
  color: "#9f9d9d",
  marginTop: -20,
  marginBottom: 10,
  cursor: "pointer",
});

const titleArea = css({
  width: "100%",
  marginLeft: 50,
});

const titleCss = css({
  width: "69%",
});

const spanTitle = css({
  color: "#bdbbbb",
});

export { span, titleArea, titleCss, spanTitle };
