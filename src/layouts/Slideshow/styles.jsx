// emotion
import { StyleSheet } from "react-native";

const slideShowCss = StyleSheet.create({
  slideshow: {
    flexDirection: "row",
    overflowX: "scroll",
    margin: "auto",
  },
});
export default slideShowCss;
