// emotion
import { StyleSheet } from "react-native";

const slideShowCss = StyleSheet.create({
  slideshow: {
    flexDirection: "row",
    margin: "auto",
    overflow: "scroll",
  },
});
export default slideShowCss;
