// emotion
import { StyleSheet } from "react-native";

const museum = StyleSheet.create({
  container: {
    marginBottom: 10,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
    width: "95%",
  },
});

export { museum };
