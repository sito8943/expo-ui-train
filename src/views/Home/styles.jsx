// emotion
import { StyleSheet } from "react-native";

const span = StyleSheet.create({
  color: "#9f9d9d",
  marginTop: -20,
  marginBottom: 10,
  cursor: "pointer",
});

const titleArea = StyleSheet.create({
  width: "100%",
  marginLeft: 50,
});

const titleCss = StyleSheet.create({
  width: "69%",
});

const spanTitle = StyleSheet.create({
  color: "#bdbbbb",
});

const centerRow = StyleSheet.create({
  display: "flex",
  justifyContent: "center",
});

export { span, titleArea, titleCss, spanTitle, centerRow };
